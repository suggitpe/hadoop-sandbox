apply plugin: 'java'

jar{
    baseName = 'sandbox-mapreduce'
}

dependencies{
    compile libs.hadoopclient

    testCompile libs.test.junit,
            libs.test.mrunit,
            libs.test.hadoopMiniCluster

}
