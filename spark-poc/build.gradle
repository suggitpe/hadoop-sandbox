apply plugin: 'scala'

jar{
    baseName = 'spark-poc'
}

dependencies{
    compile libs.scalalang,
            libs.spark,
            libs.sparkSql

    runtime libs.sparkAvro

    testCompile libs.test.scalatest
}